<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profil bearbeiten</title>
    <%= stylesheet_link_tag "home", media: "all", "data-turbo-track": "reload" %>
</head>
<body>

    <div class="profile-edit-container">
        <h1>Profil bearbeiten</h1>

        <%= form_with model: @user, url: profile_path, local: true do |f| %>
            <div class="form-group">
                <%= f.label :name, "Benutzername", class: "form-label" %>
                <%= f.text_field :name, class: "form-control" %>
            </div>

            <div class="form-group">
                <%= f.label :email, "E-Mail", class: "form-label" %>
                <%= f.email_field :email, class: "form-control" %>
            </div>

            <div class="form-group">
                <%= f.label :current_password, "Aktuelles Passwort (erforderlich bei Passwortänderung)", class: "form-label" %>
                <%= f.password_field :current_password, class: "form-control" %>
            </div>

            <div class="form-group">
                <%= f.label :password, "Neues Passwort (optional)", class: "form-label" %>
                <%= f.password_field :password, class: "form-control" %>
            </div>

            <div class="form-group">
                <%= f.label :password_confirmation, "Neues Passwort bestätigen", class: "form-label" %>
                <%= f.password_field :password_confirmation, class: "form-control" %>
            </div>

            <div class="form-buttons">
                <%= f.submit 'Profil aktualisieren', class: "custom-btn btn-primary" %>
                <%= link_to 'Zurück zum Profil', profile_path, class: "custom-btn btn-secondary" %>
            </div>
        <% end %>

    </div>

</body>
</html>
