<!DOCTYPE html>
<html lang="de">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ressource bearbeiten</title>
    <%= stylesheet_link_tag "home", media: "all", "data-turbo-track": "reload" %>
  </head>
  <body>

    <div class="edit-resource-container">
      <h1>Ressource bearbeiten</h1>

      <%= form_with(model: [@group, @resource], local: true) do |form| %>
        <div class="form-group">
          <%= form.label :name, "Name der Ressource", class: "form-label" %>
          <%= form.text_field :name, class: "form-control" %>
        </div>

        <div class="form-group">
          <%= form.label :description, "Beschreibung", class: "form-label" %>
          <%= form.text_area :description, class: "form-control" %>
        </div>

        <div class="form-group">
          <%= form.label :quantity, "Menge", class: "form-label" %>
          <%= form.number_field :quantity, class: "form-control" %>
        </div>

        <div class="form-buttons">
          <%= form.submit "Speichern", class: "custom-btn btn-success" %>
          <%= link_to 'Zurück', group_resources_path(@group), class: "custom-btn btn-secondary" %>
        </div>
      <% end %>

    </div>

  </body>
</html>
