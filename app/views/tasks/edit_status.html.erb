<!DOCTYPE html>
<html lang="de">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Status für Aufgabe: <%= @task.title %></title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <%= stylesheet_link_tag "home", media: "all", "data-turbo-track": "reload" %>
  </head>
  <body>

    <div class="container">
      <h1 class="mb-4">Status für Aufgabe: <%= @task.title %></h1>

      <%= form_with(model: [@group, @task], url: update_status_group_task_path(@group, @task), method: :patch, class: "needs-validation") do |form| %>
        <div class="form-group mb-3">
          <%= form.label :status, 'Status', class: 'form-label' %>
          <%= form.select :status, Task.statuses.keys.map { |status| [status.humanize, status] }, {}, class: 'form-select' %>
        </div>

        <div class="form-group mb-3">
          <%= form.label :status_comment, 'Kommentar zum Status', class: 'form-label' %>
          <%= form.text_area :status_comment, class: 'form-control', rows: 3 %>
        </div>

        <div class="d-flex gap-2">
          <%= form.submit 'Status aktualisieren', class: 'btn btn-primary' %>
          <%= link_to 'Zurück', group_tasks_path(@group), class: 'btn btn-secondary' %>
        </div>
      <% end %>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
