<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login | HomeAssist</title>
  <%= stylesheet_link_tag "landing_page", media: "all", "data-turbo-track": "reload" %>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

  <div class="container mt-5">
    <!-- Login-Formular in einem Rahmen -->
    <div class="row justify-content-center">
      <div class="col-md-6"> <!-- Anpassung der Breite -->
        <div class="form-container">
                  <!-- Navbar -->
          <nav class="navbar navbar-expand-lg navbar-custom">
           <div class="container">
            <a class="navbar-brand" href="#">
             <%= image_tag 'logo.jpg', alt: 'HomeAssist Logo', class: 'navbar-logo' %>
            </a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
             <span class="navbar-toggler-icon"></span>
             </button>
            <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ml-auto">
             <li class="nav-item">
              <%= link_to 'Home', root_path, class: 'nav-link btn btn-outline-primary px-3 mx-2' %>
             </li>
            </ul>
          </div>
        </div>
      </nav>
       <br>
          <h1 class="form-title text-center">Login</h1>

          <!-- Flash-Alert-Nachricht anzeigen -->
          <% if flash[:alert] %>
            <div class="alert alert-danger">
              <%= flash[:alert] %>
            </div>
          <% end %>

          <!-- Login-Formular -->
          <%= form_with url: login_path, local: true do |f| %>
            <div class="form-group">
              <%= f.label :email, "E-Mail" %>
              <%= f.email_field :email, class: "form-control" %>
            </div>

            <div class="form-group">
              <%= f.label :password, "Passwort" %>
              <%= f.password_field :password, class: "form-control" %>
            </div>

            <%= f.submit 'Login', class: "btn btn-primary btn-block" %>
          <% end %>

          <p class="mt-3 text-center">Noch kein Konto? <%= link_to 'Registrieren', new_registration_path %></p>
        </div>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS und jQuery (optional für interaktive Komponenten) -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.2/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

</body>
</html>
